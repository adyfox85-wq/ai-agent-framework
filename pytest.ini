[pytest]
markers =
    gui_e2e: real-desktop GUI E2E tests (drive real Tk windows / real clipboard / real hotkeys; require an interactive desktop; excluded from default automated runs)
addopts = -m "not gui_e2e"
